<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenido</title>
    <link rel="stylesheet" type="text/css" href="style.css">
</head>
<body>
    <header>
        <h1>Gestión de Reservas para <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Lista de Reservas</h2>
            <ul class="list">
                <% data.reservas.forEach(reserva => { %>
                    <li class="item">
                        <strong><%= reserva.fecha %></strong>
                        <span>Hora de Inicio: <%= reserva.horaInicio %></span>
                        <span>Hora de Fin: <%= reserva.horaFin %></span>
                        <a href="/reservas/<%=reserva.id_reserva%>">Ver detalles...</a><br>
                        <a href="/reservas/editarReservas/<%=reserva.id_reserva%>">Editar Reserva</a>

                    </li>
                <% }) %>
            </ul>
        </section>
    </main>
</body>
</html>