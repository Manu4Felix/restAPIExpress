<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Editar Reserva</title>
  <link rel="stylesheet" href="/styleForm.css" />
</head>
<body>
  <header>
    <h1>Editar Reserva</h1>
  </header>

  <main>
    <h2>Modifica los datos de la reserva:</h2>
    <form action="/reservas/<%= reserva.id_reserva %>?_method=PUT" method="POST">

      <label for="fecha">Fecha:</label>
      <input type="date" id="fecha" name="fecha" value="<%= reserva.fecha %>" required>

      <label for="hora_inicio">Hora de Inicio:</label>
      <input type="time" id="hora_inicio" name="hora_inicio" value="<%= reserva.hora_inicio %>" required>

      <label for="hora_fin">Hora de Fin:</label>
      <input type="time" id="hora_fin" name="hora_fin" value="<%= reserva.hora_fin %>" required>

      <label for="estado">Estado:</label>
      <input type="text" id="estado" name="estado" value="<%= reserva.estado %>" required>

      <button type="submit">Guardar cambios</button>
    </form>

    <p>
      <a href="/reservas" class="back-button">
        ← Volver a Lista de Reservas
      </a>
    </p>
  </main>
</body>
</html>
