<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenido</title>
    <link rel="stylesheet" type="text/css" href="style.css">
</head>

<body>
    <header>
        <h1>Gestión de Notificaciones para <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Lista de Notificaciones</h2>
            <ul class="notificacion-list">
                <% data.notificaciones.forEach(notificacion=> { %>
                    <li class="notificacion-item">
                        <strong>
                            Tipo: <%= notificacion.tipo %>
                        </strong>
                        <span>Fecha: <%= notificacion.fecha %></span>
                        <span>Hora: <%= notificacion.hora %></span>
                        <span>Estado: <%= notificacion.estado %></span>
                        <a href="/notificaciones/<%=notificacion.id_notificacion%>">Ver detalles...</a>
                    </li>

                    <% }) %>
            </ul>
        </section>
    </main>
</body>

</html>